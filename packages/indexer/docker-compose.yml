services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nightmint
      POSTGRES_PASSWORD: nightmint
      POSTGRES_DB: nightmint_indexer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
